/**
 * Tailwind v4 is CSS-first: theme configuration lives in src/app/globals.css
 * via `@theme`, so there is deliberately no tailwind.config.js in this repo.
 */
const config = {
  plugins: ['@tailwindcss/postcss'],
}

export default config
