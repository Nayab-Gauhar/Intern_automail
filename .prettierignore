node_modules
.next
bun.lock
prisma/migrations
coverage
playwright-report
test-results
.pgdata

# Architecture docs are prose with hand-aligned ASCII diagrams and tables that
# Prettier's markdown printer would mangle. Reviewed by humans, not formatters.
docs/**/*.md
*.md

# The design-token block is a hand-aligned table transcribed from
# docs/architecture/04-design-system.md, which is the spec of record. Prettier
# collapses the alignment and lowercases every hex value, producing a 419-line
# diff that makes the file impossible to check against the spec.
src/app/globals.css
